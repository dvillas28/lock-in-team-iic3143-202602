# Skills Del Repo

Skills locales en `.agents/skills/`:

- `lms-reviewer`: revision de diffs centrada en bugs, seguridad y regresiones.
- `lms-db-design`: convenciones de datos para multi-tenancy academico.
- `lms-ui-ux`: UX basica para estudiantes, docentes y admins.

Skill externo revisado:

- `nextlevelbuilder/ui-ux-pro-max-skill`: util para UI/UX amplio. No se instala
  en este repo porque requiere CLI/plugin externo; usarlo cuando ya definamos
  stack visual o queramos generar un sistema de diseno.
